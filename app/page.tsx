import ListView from "@/components/layout/ListView";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <ListView />
    </div>
  );
};

export default Home;
