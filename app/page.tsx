import ListView from "@/components/layout/ListView";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <h1>Home Page</h1>
      <ListView />
    </div>
  );
};

export default Home;
